#!/usr/bin/env bash

BUILD_DIR=$1
CACHE_DIR=$2
BIN_DIR=$(cd $(dirname $0); pwd)
BP_DIR=$(cd $(dirname $0); cd ..; pwd)

#PSPELL適用
source $BP_DIR/inc/apply_pspell.sh

#WHTMLTOPDF適用
source $BP_DIR/inc/apply_pdf.sh


